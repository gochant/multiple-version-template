/**
 * 模块元数据文件
 */

define({
    name: 'sample',  // 模块名称，通常与模块文件夹名保持一致
    path: 'product/modules/${name}/widgets',  // 模块内 widgets 路径
    multilevel: true  // widgets 的放置方式：单层级（false）还是多层级（true）
});